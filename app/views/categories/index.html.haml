%h3 Listing categories
%p#notice= notice
%p#error= flash[:error]
%br
%table.table.table-hover
  %tr
    %th.col-sm-10 Name
    %th.col-sm-1
    %th.col-sm-1

  - categories.each do |category|
    %tr
      %td= link_to category.name, category
      %td= link_to 'Edit', edit_category_path(category) if current_user && current_user.admin?
      %td= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' } if current_user && current_user.admin?

%br
